language: php

php:
  - '7.0'
  
cache:
  directories:
    - $HOME/.composer/cache
    
addons:
  sonarcloud:
    organization: kimfml
    token:
      secure: ••••••••••••••••
 
git:
  depth: false 

env:
  - SYMFONY_VERSION="dev-master" DB=mysql

install: composer update --no-interaction --no-suggest --no-progress

before_script: composer require atoum/atoum
script:
  - phpunit --coverage-clover=coverage-report.clover 
  - phpunit --log-junit=test-report.xml
  - sonar-scanner
  
        




